pre-commit:
  parallel: true

  commands:
    eslint:
      glob: "*.{ts,tsx}"
      run: pnpm eslint --fix "{staged_files}"
      stage_fixed: true

    typecheck:
      run: pnpm typecheck

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit "$1"
